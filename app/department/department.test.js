'use strict';

describe('department module', function() {

  beforeEach(module('department'));

  describe('department controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var departmentCtrl = $controller('departmentCtrl');
      expect(departmentCtrl).toBeDefined();
    }));

  });
});